# Ignore environment variables file
.env
# Ignore node_modules folder
/node_modules
# Ignore local data dumps (JSON/CSV extracts). Small lookups already
# tracked under data/ stay tracked; new dumps will not be committed.
/data
/consumer/*.csv
.vercel
.env*
